#include <iostream>
#include <stack>
using namespace std;
void Reverse(stack<int> &st) {
    // Base Case :
    if(st.empty()) {
        return;
    }
    int num = st.top();
    st.pop();

    // Recursive call:
    fd
}
    
int main() {
    stack <int> st;
    
    
    return 0;
}